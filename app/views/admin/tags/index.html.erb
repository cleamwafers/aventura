<h2>タグ一覧・追加</h2>

<%= form_with model: @tag, url: admin_tags_path do |f| %>
  <table class="table table-borderless">
    <tbody>
      <tr>
        <td>
          タグ名
        </td>
        <td>
          <%= f.text_field :tag_name　%>
        </td>
        <td>
          <%= f.submit "新規登録", class: "btn btn-success" %>
        </td>
      </tr>
    </tbody>
  </table>
<% end %>

<table class="table">
  <thead>
    <tr>
      <th>
        タグ名
      </th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @tags.each do |tag_name| %>
    <tr>
      <td>
        <%= tag.tag_name %>
      </td>
      <td>
        <%= link_to "編集する", edit_admin_tag_path(genre), class: "btn btn-success" %>
      </td>
    </tr>
    <% end %>
  </tbody>
</table>
