<div class="row">
	<div class="col-xs-offset-1 col-xs-8">
<h4>登録情報 <%= link_to"編集する", edit_user_user_path,class: "btn btn-success" %></h4>
<div class="mypage-bottoms">
<h4>投稿
<%= link_to new_user_sanctuary_path do %>
 <i class="fas fa-plus"></i>
<% end %>
<%= link_to"一覧を見る", user_sanctuarys_path, class: "btn btn-primary" %>
</h4>
</div>

<table border="1" cellspacing="0" cellpadding="0" >
  <tr>
   <th>ユーザー名</th>
   <td><%= @user.name %><td>
  </tr>
   <tr>
   <th>メールアドレス</th>
   <td><%= @user.email %><td>
  </tr>
  <tr>
  <th>sns</th>
   <td><%= @user.sns %><td>
   <%# link_to? %>
  </tr>
  <th>プロフィール画像</th>
   <td>
   <%= image_tag @user.get_image(100,100) %>
   <td>
  </tr>
  <tr>
   <th>自己紹介</th>
   <td><%= @user.introduction %><td>
  </tr>
</div>
	</div>

 自分のいいねした一覧
<ul>
	 <% current_user.favorites.each do |favorite| %>
	  <li><%= link_to favorite.sanctuary.name, user_sanctuary_path(favorite.sanctuary_id) %></li>
	 <% end %>
	</ul>
 自分の投稿一覧
	<ul>
	 <% current_user.sanctuarys.each do |sanctuary| %>
	  <li><%= link_to sanctuary.name, user_sanctuary_path(sanctuary) %></li>
	 <% end %>
	</ul>